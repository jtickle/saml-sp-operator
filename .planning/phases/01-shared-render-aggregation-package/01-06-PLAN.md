---
phase: 01-shared-render-aggregation-package
plan: 06
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04", "01-05"]
files_modified:
  - internal/render/inject_test.go
  - internal/render/determinism_test.go
  - internal/render/sanitize.go
  - internal/render/sanitize_test.go
autonomous: true
requirements: [RENDER-10, RENDER-09]
must_haves:
  truths:
    - "Hostile CRD string fields (embedded <, &, --, ]]>, quotes) routed through every render entrypoint produce well-formed XML that re-parses via encoding/xml with no error and no FATAL-ing structure (RENDER-10, ROADMAP crit 3)"
    - "A CRD-derived string routed into an XML comment is stripped of -- at the input layer so it can never abort the whole render (D-05)"
    - "Rendering the same SPConfig + shuffled []AppBinding twice produces byte-identical output and an identical config hash across all three files (RENDER-09 reorder-stability, ROADMAP crit 4)"
  artifacts:
    - internal/render/inject_test.go
    - internal/render/determinism_test.go
    - internal/render/sanitize.go
  key_links:
    - "This plan is the adversarial + determinism cross-check over the whole render pipeline built in plans 01-05 — it proves the injection-safety and hash-stability acceptance criteria end to end"
    - "The -- input-strip (sanitize.go) is applied by the XML renderers before any comment marshaling (D-05 defense-in-depth)"
---

<objective>
Prove the whole render pipeline is injection-safe (RENDER-10) and deterministic/reorder-stable (RENDER-09, ROADMAP crit 4) with adversarial and property tests over the real renderers from plans 01-05, and add the input-layer `--` sanitizer (D-05) the XML renderers depend on.

Purpose: RENDER-10 is a cross-cutting guarantee best proven by throwing hostile inputs at every entrypoint and re-parsing the output; RENDER-09's reorder-stability is a property of the full pipeline (no map ranging anywhere), only testable once all renderers exist.
Output: `sanitize.go` (the `--` strip helper) + `inject_test.go` (adversarial fuzz) + `determinism_test.go` (render-twice / shuffle-order byte + hash identity).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-render-aggregation-package/01-CONTEXT.md
@.planning/phases/01-shared-render-aggregation-package/01-RESEARCH.md
@.planning/phases/01-shared-render-aggregation-package/01-PATTERNS.md
@.planning/phases/01-shared-render-aggregation-package/01-VALIDATION.md
</context>

## Artifacts this phase produces

The Phase 1 `internal/render` package (all plans) creates these NEW symbols — canonical list for the source-grounding drift pass:

- Exported types: `render.SPConfig`, `render.IdPConfig`, `render.AttributeMapping`, `render.AppBinding`, `render.Resolution`, `render.Conflict`, `render.SelfURL`, `render.AttachmentModel` (+ consts `render.TraefikForwardAuth`, `render.NginxAuthRequest`), `render.ClearListSpec`, `render.ConfigFile`
- Exported funcs: `render.Resolve`, `render.DeriveSelfURL`, `render.RenderShibboleth2`, `render.RenderAttributeMap`, `render.RenderNginxConf`, `render.ClearList`, `render.Hash`
- Unexported helpers: `collapseEmptyElements`, `rankOrder`, `validateHostname`, `buildShibboleth2Tree`, `sanitizeComment`
- New field: `AppBinding.Priority int32`
- New files: the `internal/render/*.go` set + `testdata/golden/*` + `shibdload_test.go`
- New build tag: `shibdload`

This plan (06) creates: `sanitize.go` (`sanitizeComment`), `inject_test.go`, `determinism_test.go`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Input-layer -- sanitizer (sanitize.go) + wire into XML comment paths — D-05</name>
  <files>internal/render/sanitize.go, internal/render/sanitize_test.go</files>
  <read_first>
    - .planning/phases/01-shared-render-aggregation-package/01-CONTEXT.md (D-05 — strip -- at the input layer via strings.ReplaceAll(v,"--","-") before marshaling)
    - .planning/phases/01-shared-render-aggregation-package/01-RESEARCH.md (Pitfall 4 — the two comment-validation paths; the struct-tag ,comment path rejects any --)
    - internal/render/shibboleth2.go (any operator-generated comment routed through a ,comment struct field)
  </read_first>
  <behavior>
    - sanitizeComment("a--b----c") contains no "--" substring
    - A value already free of "--" is returned unchanged
  </behavior>
  <action>
    Create sanitize.go with sanitizeComment(v string) string collapsing every "--" to "-" (strings.ReplaceAll applied until no "--" remains, since a naive single pass over "---" leaves "--") so the result can never contain "--". If shibboleth2.go (plan 03) routes any CRD-derived string into a comment, apply sanitizeComment to that value before it reaches the struct-tag `,comment` field — this is defense-in-depth so a single hostile field is silently sanitized rather than aborting the entire render (D-05). Do not switch comment rendering to low-level EncodeToken (which only rejects -->; RESEARCH Pitfall 4). In sanitize_test.go assert the output of sanitizeComment on "----", "a--b", and "---" contains no "--" substring.
  </action>
  <verify>
    <automated>go test ./internal/render/... -run TestSanitizeComment</automated>
  </verify>
  <acceptance_criteria>
    - sanitizeComment guarantees no "--" remains in its output for any input (including odd-length runs like "---")
    - Any comment path in the XML renderers applies sanitizeComment before the ,comment struct field
    - `go test ./internal/render/... -run TestSanitizeComment` exits 0
  </acceptance_criteria>
  <done>Any CRD string routed into an XML comment is guaranteed "--"-free at the input layer, so a hostile field can never abort the render.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adversarial injection safety across all renderers (inject_test.go) — RENDER-10</name>
  <files>internal/render/inject_test.go</files>
  <read_first>
    - .planning/phases/01-shared-render-aggregation-package/01-VALIDATION.md (RENDER-10 row — hostile strings never produce invalid/FATAL-ing XML)
    - .planning/phases/01-shared-render-aggregation-package/01-RESEARCH.md (Security Domain threat table; Anti-Patterns)
    - internal/render/shibboleth2.go, internal/render/attributemap.go (the entrypoints under test)
  </read_first>
  <behavior>
    - For each hostile token (<, >, &, ", ', --, ]]>, a NUL-free control string) injected into every string field of SPConfig/AppBinding/AttributeMapping, RenderShibboleth2 and RenderAttributeMap return no error
    - The rendered bytes re-parse successfully via xml.Unmarshal / xml.NewDecoder with no error (proves well-formedness)
    - The hostile token never appears un-escaped in a structural position (e.g. a raw < that opens a spurious element)
  </behavior>
  <action>
    Add TestInjectionSafety in inject_test.go iterating a table of hostile tokens and injecting each into every string field of the sample SPConfig, AppBinding, and AttributeMapping (entityID, hostname, path, attribute name/id, REMOTE_USER, credential paths). For each case call RenderShibboleth2 and RenderAttributeMap and assert: (a) no error is returned (the -- input-strip + auto-escaping keep marshaling from aborting); (b) the output re-parses via an xml.Decoder Token loop or xml.Unmarshal into a generic structure with no error (well-formed); (c) the injected token does not appear as unescaped structural XML. Also assert a hostile hostname passed to RenderNginxConf is rejected with an error (the validateHostname allowlist from plan 05). Do not add a negative grep for a literal token against a written source file — assert via parse success and escape checks in-test.
  </action>
  <verify>
    <automated>go test ./internal/render/... -run TestInjectionSafety</automated>
  </verify>
  <acceptance_criteria>
    - TestInjectionSafety injects <, >, &, ", ', --, ]]> into every string field and asserts no marshal error from the XML renderers
    - Rendered output re-parses via encoding/xml with no error for every hostile case (well-formedness proven)
    - A hostile hostname to RenderNginxConf returns an error (allowlist rejection)
    - `go test ./internal/render/... -run TestInjectionSafety` exits 0
  </acceptance_criteria>
  <done>Hostile CRD strings can never produce invalid/FATAL-ing XML nor break nginx.conf — proven adversarially across every entrypoint (ROADMAP crit 3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pipeline determinism + hash reorder-stability (determinism_test.go) — RENDER-09</name>
  <files>internal/render/determinism_test.go</files>
  <read_first>
    - .planning/phases/01-shared-render-aggregation-package/01-VALIDATION.md (RENDER-09 row — hash stable across map-order reshuffles, changes iff content changes)
    - .planning/phases/01-shared-render-aggregation-package/01-RESEARCH.md (Anti-Pattern: never range a Go map in the render path; Pattern 3 confighash)
    - internal/render/confighash.go, internal/render/resolve.go, internal/render/shibboleth2.go
  </read_first>
  <behavior>
    - Rendering the same SPConfig + []AppBinding twice yields byte-identical shibboleth2.xml, attribute-map.xml, nginx.conf
    - Shuffling the []AppBinding input order yields an identical config hash (Resolve + render are order-independent)
    - Changing a semantically-meaningful field (e.g. an attribute id, or an entityID) changes the config hash; reordering alone does not
  </behavior>
  <action>
    Add TestConfigHashStability in determinism_test.go that: (a) renders all three artifacts from a fixed sample twice and asserts byte-identical output each time; (b) shuffles the []AppBinding slice N times, re-runs Resolve + all three renderers + Hash(shibboleth2.xml, nginx.conf, attribute-map.xml in fixed order) and asserts the hash is identical every run (reorder-stable — ROADMAP crit 4); (c) mutates a semantic field (an attribute id and, separately, the entityID) and asserts the hash changes. This is the end-to-end tie between confighash (plan 02) and the real renderers (plans 03/04/05), proving no map-ranging leaked nondeterminism into the pipeline.
  </action>
  <verify>
    <automated>go test ./internal/render/... -run TestConfigHashStability</automated>
  </verify>
  <acceptance_criteria>
    - Rendering twice from the same input is byte-identical for all three files
    - The config hash is identical across at least 50 input-order shuffles (reorder-stable)
    - A semantic-field change (attribute id / entityID) changes the hash
    - `go test ./internal/render/... -run TestConfigHashStability` exits 0
  </acceptance_criteria>
  <done>The full render pipeline is deterministic and the config hash changes iff semantic content changes — reorder alone never perturbs it (ROADMAP crit 4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| all CRD string fields → all rendered artifacts | Every tenant/operator-supplied string is adversarially exercised through the full render pipeline here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | XML injection / malformed output from any hostile CRD string field | high | mitigate | TestInjectionSafety adversarially injects <, &, --, ]]>, quotes into every field and asserts the output re-parses well-formed with no marshal error — the end-to-end proof of RENDER-10 (D-05 auto-escape + -- strip). |
| T-06-02 | Denial of Service | A single hostile field aborting the entire render (marshal error on --) | medium | mitigate | sanitizeComment collapses -- at the input layer (D-05) so one hostile value is silently sanitized, never aborting config generation for the whole SP. |
| T-06-03 | Tampering | Nondeterministic render output (leaked map iteration) defeating hash-gated rollout | medium | mitigate | TestConfigHashStability asserts byte-identical render + identical hash across 50+ input-order shuffles, proving no map ranging leaked into the pipeline (ROADMAP crit 4). |
</threat_model>

<verification>
- `go test ./internal/render/... -run 'TestSanitizeComment|TestInjectionSafety|TestConfigHashStability'` exits 0
- Full hermetic suite green: `go test ./internal/render/...`
</verification>

<success_criteria>
- RENDER-10: hostile CRD strings never produce invalid/FATAL-ing XML nor break nginx.conf (ROADMAP crit 3)
- RENDER-09: config hash is reorder-stable and change-sensitive end to end (ROADMAP crit 4)
</success_criteria>

<output>
Create `.planning/phases/01-shared-render-aggregation-package/01-06-SUMMARY.md` when done
</output>
