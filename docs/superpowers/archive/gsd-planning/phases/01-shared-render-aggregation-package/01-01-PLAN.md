---
phase: 01-shared-render-aggregation-package
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/render/types.go
  - internal/render/selfurl.go
  - internal/render/selfurl_test.go
  - internal/render/resolve.go
  - internal/render/resolve_test.go
  - internal/render/fixtures_test.go
autonomous: true
requirements: [RENDER-02, RENDER-06]
must_haves:
  truths:
    - "Resolve(bindings) picks the same winner for a colliding (hostname,path) on every run regardless of input slice order (D-06/D-07, ROADMAP crit 2)"
    - "Two bindings with an identical whole-second CreatedAtUnix and different UIDs deterministically resolve to the lower-UID winner (D-07 UID tiebreak, ROADMAP crit 2)"
    - "The collision loser is excluded from Winners and carried in Conflicts with {Winner, Loser ns/name/UID, Hostname, Path} (D-06)"
    - "DeriveSelfURL turns an app external URL into consistent {Scheme, Name, Port, HandlerURL} where HandlerURL is fully qualified incl. non-standard port (RENDER-02, spike fix M)"
  artifacts:
    - internal/render/types.go
    - internal/render/selfurl.go
    - internal/render/resolve.go
  key_links:
    - "Resolve is THE single collision function both controllers will import (Phase 3/4/5) — this decomposition is the 'controllers can never disagree' guarantee (D-06)"
    - "AppBinding.Priority int32 + CreatedAtUnix int64 + UID string are the synthesized inputs the controllers populate from ObjectMeta/HTTPRoute — the render package never imports k8s (D-01)"
---

<objective>
Establish the pure-Go input/output model for `internal/render` and the two pure computations over it: self-URL derivation (RENDER-02) and deterministic collision resolution (RENDER-06). This is the highest-leverage plan in the phase — `Resolve` is the shared seam both controllers import so they can never disagree about a collision winner (D-06).

Purpose: Every other plan in this phase consumes these types and functions. Getting the sort key, the UID tiebreak, and the k8s-free boundary right here is what makes the whole package correct and reusable.
Output: `types.go` (all plain-Go structs), `selfurl.go` + test, `resolve.go` + determinism test, and shared `fixtures_test.go` sample literals.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-render-aggregation-package/01-CONTEXT.md
@.planning/phases/01-shared-render-aggregation-package/01-RESEARCH.md
@.planning/phases/01-shared-render-aggregation-package/01-PATTERNS.md
@.planning/phases/01-shared-render-aggregation-package/01-VALIDATION.md

# CRD semantic vocabulary the plain-Go structs MIRROR but MUST NOT import
# (read via: git show gsd/operator-scaffold:api/v1alpha1/appintegration_types.go
#            git show gsd/operator-scaffold:api/v1alpha1/spinstance_types.go)
</context>

## Artifacts this phase produces

The Phase 1 `internal/render` package (all plans) creates these NEW symbols. This section is the canonical list for the source-grounding drift pass — every symbol below is created in this phase, not pre-existing.

- Exported types: `render.SPConfig`, `render.IdPConfig`, `render.AttributeMapping`, `render.AppBinding`, `render.Resolution`, `render.Conflict`, `render.SelfURL`, `render.AttachmentModel` (+ consts `render.TraefikForwardAuth`, `render.NginxAuthRequest`), `render.ClearListSpec`, `render.ConfigFile`
- Exported funcs: `render.Resolve`, `render.DeriveSelfURL`, `render.RenderShibboleth2`, `render.RenderAttributeMap`, `render.RenderNginxConf`, `render.ClearList`, `render.Hash`
- Unexported helpers: `collapseEmptyElements`, `rankOrder`, `validateHostname`, `buildShibboleth2Tree`
- New field: `AppBinding.Priority int32` (mirrors a future `AppIntegration.spec.priority int32`, default 0, higher wins — D-07)
- New files: `internal/render/{types,selfurl,resolve,shibboleth2,attributemap,nginxconf,clearlist,xmlformat,confighash}.go` + `_test.go` peers, `internal/render/testdata/golden/{shibboleth2.xml,attribute-map.xml,nginx.conf}`, `internal/render/testdata/fixtures/`, `internal/render/shibdload_test.go`
- New build tag: `shibdload`

This plan (01) creates: `types.go`, `selfurl.go`, `resolve.go`, and shared `fixtures_test.go`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Plain-Go input/output types (types.go) + shared test fixtures</name>
  <files>internal/render/types.go, internal/render/fixtures_test.go</files>
  <read_first>
    - .planning/phases/01-shared-render-aggregation-package/01-CONTEXT.md (D-01, D-02, D-06, D-07 — the plain-Go, k8s-free boundary)
    - .planning/phases/01-shared-render-aggregation-package/01-PATTERNS.md ("types.go" section — the CRD-field → plain-Go mapping table)
    - CRD types via `git show gsd/operator-scaffold:api/v1alpha1/appintegration_types.go` and `git show gsd/operator-scaffold:api/v1alpha1/spinstance_types.go` (semantic vocabulary ONLY — never import)
  </read_first>
  <behavior>
    - A minimal SPConfig + one AppBinding literal compiles against the declared struct fields (fixtures_test.go proves the shapes are usable)
    - `go list -deps ./internal/render/` contains no k8s.io or api/v1alpha1 dependency
  </behavior>
  <action>
    Create package `render` in internal/render/types.go with plain-Go structs mirroring the CRD semantic vocabulary WITHOUT importing api/v1alpha1 or any apimachinery type (D-01/D-02). Define: SPConfig{EntityID string, IdP IdPConfig, CredentialKeyPath, CredentialCertPath string, RemoteUser []string, Sessions SessionDefaults, ExternalURL string}; IdPConfig{MetadataURL, EntityID string} (signing-cert file path is a caller-resolved path field, never fetched here); AttributeMapping{Name, Header string}; AppBinding{Namespace, Name, UID string, Hostname, Path, Scheme string, Port int, Priority int32, CreatedAtUnix int64, RequireSession bool, Attributes []AttributeMapping}; Resolution{Winners []AppBinding, Conflicts []Conflict}; Conflict{Winner AppBinding, LoserNamespace, LoserName, LoserUID, Hostname, Path string}; AttachmentModel string with consts TraefikForwardAuth and NginxAuthRequest; ClearListSpec{Model AttachmentModel, Headers []string, Glob string}; ConfigFile{Name string, Bytes []byte}. Use `int64` unix-seconds for creation time (per PATTERNS Pitfall 3 — the caller truncates metav1.Time to whole seconds at the k8s boundary so this package never sees metav1.Time). UID is a bare string, not types.UID. Add package doc noting the zero-k8s-dependency contract (D-01/D-02). In fixtures_test.go (package render), export helper builders returning a sample valid SPConfig and a sample []AppBinding (two colliding bindings + non-colliding ones) reused by every _test.go in this package.
  </action>
  <verify>
    <automated>go build ./internal/render/ && go vet ./internal/render/ && test -z "$(go list -deps ./internal/render/ | grep -E 'k8s.io|api/v1alpha1')"</automated>
  </verify>
  <acceptance_criteria>
    - internal/render/types.go declares SPConfig, IdPConfig, AttributeMapping, AppBinding, Resolution, Conflict, SelfURL-consumers, AttachmentModel (+ both consts), ClearListSpec, ConfigFile as exported types
    - AppBinding carries Priority int32, CreatedAtUnix int64, UID string
    - `go list -deps ./internal/render/` output contains zero lines matching k8s.io or api/v1alpha1
    - fixtures_test.go provides shared sample SPConfig and []AppBinding builders in package render
  </acceptance_criteria>
  <done>The package compiles and vets clean, its dependency graph is pure stdlib (no k8s.io/api/v1alpha1), and shared test fixtures exist for downstream plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Self-URL derivation (selfurl.go) — RENDER-02</name>
  <files>internal/render/selfurl.go, internal/render/selfurl_test.go</files>
  <read_first>
    - shibboleth2.xml (repo root, branch spike) — lines 48, 67-69 (fully-qualified handlerURL incl. :30443, entityID host), the SP_HOST_PLACEHOLDER + external-port shape
    - nginx.conf (repo root) — lines 52-69 (SERVER_PORT / HTTP_HOST $host:30443 — the same external port must feed both files)
    - .planning/phases/01-shared-render-aggregation-package/01-CONTEXT.md (D-04 — spike fixes M and N; the gate fails OPEN otherwise)
    - .planning/phases/01-shared-render-aggregation-package/01-RESEARCH.md (Architectural Responsibility Map — RENDER-02 is a VALUE derivation, not an env-var render)
  </read_first>
  <behavior>
    - DeriveSelfURL("https://app.example.com:30443") returns Scheme "https", Name "app.example.com", Port 30443, HandlerURL "https://app.example.com:30443/Shibboleth.sso"
    - A default-port external URL ("https://app.example.com") still yields an explicit Port 443 and a fully-qualified HandlerURL (never a relative "/Shibboleth.sso")
    - An external URL that does not parse, or is non-https, returns an error (do not silently default)
  </behavior>
  <action>
    Add SelfURL{Scheme, Name string, Port int, HandlerURL string} to types.go OR selfurl.go, and DeriveSelfURL(externalURL string) (SelfURL, error) parsing via net/url. Derive Scheme and Name from the parsed URL; derive Port explicitly — when the URL omits a port, set it to the scheme default (443 for https) and STILL emit it (spike fix N — never rely on bare-hostname auto-expansion). Build HandlerURL as scheme://name[:port]/Shibboleth.sso, always fully qualified including the port even when it is the scheme default (spike fix M — a relative handlerURL makes shibd normalize the port away and reject every handler). Return an error for an unparseable or non-https external URL rather than defaulting. This computed SelfURL is the single shared value consumed by both shibboleth2.go (handlerURL, entityID host) and nginxconf.go (SERVER_PORT, HTTP_HOST) — keep it one value, not two independently-typed literals.
  </action>
  <verify>
    <automated>go test ./internal/render/... -run TestSelfURLConsistency</automated>
  </verify>
  <acceptance_criteria>
    - TestSelfURLConsistency (selfurl_test.go) asserts the non-default-port case yields explicit Port and a fully-qualified HandlerURL carrying that port
    - A default-port (443) input still produces an explicit Port 443 and a port-bearing HandlerURL
    - A non-https or unparseable external URL returns a non-nil error
    - `go test ./internal/render/... -run TestSelfURLConsistency` exits 0
  </acceptance_criteria>
  <done>Self-URL values are derived once, consistently, and always fully qualified — the shared input feeding both the shibboleth2.xml and nginx.conf renderers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deterministic collision resolution (resolve.go) — RENDER-06</name>
  <files>internal/render/resolve.go, internal/render/resolve_test.go</files>
  <read_first>
    - .planning/phases/01-shared-render-aggregation-package/01-CONTEXT.md (D-06 Resolve/render split; D-07 sort key priority desc, createdAt asc, UID asc; the same-second UID tiebreak is load-bearing)
    - .planning/phases/01-shared-render-aggregation-package/01-PATTERNS.md ("resolve.go" section — cmp.Or + slices.SortFunc rankOrder pattern; the SortFunc-not-SortStableFunc note)
    - .planning/phases/01-shared-render-aggregation-package/01-RESEARCH.md (Pattern 2; Pitfall 3 metav1.Time second-granularity; Anti-Pattern: never range a Go map in the render path)
  </read_first>
  <behavior>
    - Resolve over N shuffled orderings of the same []AppBinding returns byte-identical Winners ordering every time
    - Two bindings colliding on (hostname,path) with equal Priority and equal CreatedAtUnix but different UID: the lexicographically-lower UID wins; the other appears in Conflicts, not Winners
    - Higher Priority beats older CreatedAtUnix (priority is consulted first)
    - Non-colliding bindings all appear in Winners; Conflicts is empty
  </behavior>
  <action>
    Implement Resolve(bindings []AppBinding) (Resolution, error) in resolve.go (D-06). Internally rankOrder(bindings) clones the slice (slices.Clone) and sorts with slices.SortFunc + cmp.Or(cmp.Compare(b.Priority, a.Priority) /* desc */, cmp.Compare(a.CreatedAtUnix, b.CreatedAtUnix) /* asc */, cmp.Compare(a.UID, b.UID) /* asc final tiebreak */) (D-07). Then partition by the (Hostname, Path) key: the first-ranked binding for each key goes to Winners; every subsequent binding for an already-claimed key becomes a Conflict{Winner: <the winner>, LoserNamespace/LoserName/LoserUID: <this binding>, Hostname, Path}. Never range a Go map to build ordered output (Anti-Pattern — breaks hash stability); if a map is used for grouping, always emit from the ranked slice order. Add a code comment recording that SortFunc (not SortStableFunc) is correct because the UID tiebreak makes the comparator a strict total order, so a future reader does not "fix" it. In resolve_test.go add TestResolveDeterminism covering: shuffle-N-times byte-identical Winners; the same-second-CreatedAtUnix + differing-UID tiebreak explicitly; priority-beats-age; loser-excluded-and-flagged.
  </action>
  <verify>
    <automated>go test ./internal/render/... -run TestResolveDeterminism</automated>
  </verify>
  <acceptance_criteria>
    - resolve.go exports Resolve(bindings []AppBinding) (Resolution, error) using cmp.Or + slices.SortFunc with the exact key (priority desc, createdAt asc, UID asc)
    - TestResolveDeterminism includes an explicit same-second-createdAt / differing-UID case asserting the lower-UID binding wins
    - TestResolveDeterminism shuffles the input at least 100 times and asserts identical Winners ordering each run
    - The collision loser is absent from Winners and present in Conflicts with the winner referenced
    - `go test ./internal/render/... -run TestResolveDeterminism` exits 0
  </acceptance_criteria>
  <done>Resolve is a pure, deterministic, strict-total-order collision function with the UID tiebreak proven under the same-second case — the shared seam is correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → render (AppBinding) | Controllers synthesize AppBinding from cross-namespace AppIntegration ObjectMeta + resolved HTTPRoute; the `priority` field is self-asserted by the app team |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | AppBinding.Priority self-assertion in Resolve | low | accept | Per CONTEXT.md Specifics: `priority` is NOT an escalation — two teams can only collide on (hostname,path) if both already hold Gateway-API-attached HTTPRoutes for that hostname; Gateway API arbitrates hostname ownership at attachment. `priority` decides precedence among already-authorized routes, never steals a hostname. Recorded here so it is not later mistaken for a trust hole (to be lifted into the AppIntegration controller threat model in Phase 3/5). |
| T-01-02 | Tampering | Non-deterministic Resolve output (map-iteration order) | medium | mitigate | Never range a Go map to build ordered output; emit strictly from the ranked slice. TestResolveDeterminism asserts byte-identical Winners across 100+ shuffles — this feeds RENDER-09 hash stability (ROADMAP crit 4). |
</threat_model>

<verification>
- `go build ./internal/render/` and `go vet ./internal/render/` clean
- `go list -deps ./internal/render/` shows no k8s.io / api/v1alpha1 dependency (D-01/D-02 enforced by the actual dependency graph, not a comment grep)
- `go test ./internal/render/... -run 'TestSelfURLConsistency|TestResolveDeterminism'` exits 0
</verification>

<success_criteria>
- Plain-Go type model exists with zero k8s dependency (D-01/D-02)
- RENDER-02 self-URL values derived once, always fully qualified (spike fixes M/N)
- RENDER-06 collision resolution deterministic with the same-second UID tiebreak proven (ROADMAP crit 2)
</success_criteria>

<output>
Create `.planning/phases/01-shared-render-aggregation-package/01-01-SUMMARY.md` when done
</output>
