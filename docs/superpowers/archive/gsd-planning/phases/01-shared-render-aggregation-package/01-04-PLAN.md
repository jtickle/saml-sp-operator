---
phase: 01-shared-render-aggregation-package
plan: 04
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - internal/render/attributemap.go
  - internal/render/attributemap_test.go
  - internal/render/testdata/golden/attribute-map.xml
autonomous: true
requirements: [RENDER-03]
must_haves:
  truths:
    - "RenderAttributeMap produces an <Attributes> document (literal xmlns + xmlns:xsi on the root only) with one self-closing <Attribute name=... id=.../> per AppIntegration attribute (RENDER-03, ROADMAP crit 5)"
    - "Output matches the project-native golden testdata byte-for-byte (semantic tree, no hand-aligned columns)"
    - "Attribute name/id string fields are XML-escaped by construction (encoding/xml)"
  artifacts:
    - internal/render/attributemap.go
    - internal/render/testdata/golden/attribute-map.xml
  key_links:
    - "attribute-map.xml bytes feed the config hash (plan 02) because it is reloadChanges=false — an attribute-only change forces a pod roll (D-08)"
    - "Self-closing + prolog from xmlformat (plan 02); AttributeMapping type from plan 01"
---

<objective>
Render `attribute-map.xml` from an `SPConfig`/`AppBinding`'s attribute list (RENDER-03) — the SAML attribute id → exported `Variable-<id>` header mapping — via `encoding/xml` struct marshaling with the same self-closing + prolog pipeline as shibboleth2.xml.

Purpose: This file is `reloadChanges="false"` (shibd never hot-reloads it), so it must render correctly AND be covered by the config hash (D-08). It is the small, second `encoding/xml` renderer.
Output: `attributemap.go` (`RenderAttributeMap`) and a project-native golden fixture + byte-compare test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-render-aggregation-package/01-CONTEXT.md
@.planning/phases/01-shared-render-aggregation-package/01-RESEARCH.md
@.planning/phases/01-shared-render-aggregation-package/01-PATTERNS.md
@.planning/phases/01-shared-render-aggregation-package/01-VALIDATION.md

# Golden target (repo root, branch spike) — structural reference to reproduce
@attribute-map.xml
</context>

## Artifacts this phase produces

The Phase 1 `internal/render` package (all plans) creates these NEW symbols — canonical list for the source-grounding drift pass:

- Exported types: `render.SPConfig`, `render.IdPConfig`, `render.AttributeMapping`, `render.AppBinding`, `render.Resolution`, `render.Conflict`, `render.SelfURL`, `render.AttachmentModel` (+ consts `render.TraefikForwardAuth`, `render.NginxAuthRequest`), `render.ClearListSpec`, `render.ConfigFile`
- Exported funcs: `render.Resolve`, `render.DeriveSelfURL`, `render.RenderShibboleth2`, `render.RenderAttributeMap`, `render.RenderNginxConf`, `render.ClearList`, `render.Hash`
- Unexported helpers: `collapseEmptyElements`, `rankOrder`, `validateHostname`, `buildShibboleth2Tree`
- New field: `AppBinding.Priority int32`
- New files: the `internal/render/*.go` set + `testdata/golden/*` + `shibdload_test.go`
- New build tag: `shibdload`

This plan (04) creates: `attributemap.go` (`RenderAttributeMap`) and `testdata/golden/attribute-map.xml`.

## Golden-fixture scope decision (documented)

Same decision as plan 03: the project-native `testdata/golden/attribute-map.xml` is the byte-compare target (semantic attribute set/order, machine-formatted). The repo-root `attribute-map.xml`'s hand-aligned attribute columns (`name="email"     id="email"`) are cosmetic spike formatting that `encoding/xml` cannot and should not reproduce (RESEARCH Pitfall 5) — target the semantic attribute set only.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the golden fixture (testdata/golden/attribute-map.xml)</name>
  <files>internal/render/testdata/golden/attribute-map.xml</files>
  <read_first>
    - attribute-map.xml (repo root, branch spike) — lines 31-37 (the <Attributes> root xmlns+xmlns:xsi and the four self-closing <Attribute> children)
    - .planning/phases/01-shared-render-aggregation-package/01-PATTERNS.md ("attributemap.go" section — the root and child shapes)
    - .planning/phases/01-shared-render-aggregation-package/01-RESEARCH.md (Pitfall 5 — no column alignment; Pitfall 1 self-closing)
  </read_first>
  <behavior>
    - The golden fixture is the exact byte output expected from RenderAttributeMap for a fixed sample attribute list (email, firstName, lastName, id→uid)
    - It contains the XML prolog, the literal-xmlns(+xmlns:xsi) root, and self-closing <Attribute> children with single-space attribute separation (no column alignment)
  </behavior>
  <action>
    Write internal/render/testdata/golden/attribute-map.xml reproducing the repo-root fixture's semantic tree: the XML prolog, an <Attributes> root declaring xmlns (urn:mace:shibboleth:2.0:attribute-map) and xmlns:xsi (http://www.w3.org/2001/XMLSchema-instance) as literal string attributes, and one self-closing <Attribute name=... id=.../> per sample attribute in a fixed order matching the sample AttributeMapping slice. Use single-space attribute separation and 4-space indentation as xml.MarshalIndent would emit — do NOT reproduce the fixture's hand-aligned columns. Omit the spike prose comment block (lines 1-30). This is the byte target Task 2 implements against.
  </action>
  <verify>
    <automated>test -f internal/render/testdata/golden/attribute-map.xml && grep -q 'xmlns:xsi=' internal/render/testdata/golden/attribute-map.xml && grep -qE '<Attribute name="[^"]+" id="[^"]+"/>' internal/render/testdata/golden/attribute-map.xml</automated>
  </verify>
  <acceptance_criteria>
    - internal/render/testdata/golden/attribute-map.xml exists, starts with the XML prolog, declares xmlns and xmlns:xsi on the root
    - Each <Attribute> is self-closing with name and id attributes, single-space separated (no column alignment)
    - No spike prose-comment block is present
  </acceptance_criteria>
  <done>A machine-formatted golden fixture exists as the exact byte target for the attribute-map renderer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render attribute-map.xml via encoding/xml (RenderAttributeMap) — RENDER-03</name>
  <files>internal/render/attributemap.go, internal/render/attributemap_test.go</files>
  <read_first>
    - internal/render/testdata/golden/attribute-map.xml (the byte target from Task 1)
    - .planning/phases/01-shared-render-aggregation-package/01-CONTEXT.md (D-03 literal-xmlns on root only)
    - .planning/phases/01-shared-render-aggregation-package/01-PATTERNS.md ("attributemap.go" + "Shared Patterns" self-closing collapse)
  </read_first>
  <behavior>
    - RenderAttributeMap(attrs) output equals testdata/golden/attribute-map.xml byte-for-byte for the sample attribute list
    - The attribute order in the output follows the input slice order (no map ranging)
  </behavior>
  <action>
    Implement RenderAttributeMap(attrs []AttributeMapping) ([]byte, error) building an Attributes root struct with xmlns and xmlns:xsi as literal string attributes (xml:"xmlns,attr", xml:"xmlns:xsi,attr") and a slice field of Attribute{Name string xml:"name,attr", ID string xml:"id,attr"} (D-03 literal-xmlns approach, root struct only). Marshal via xml.MarshalIndent(tree, "", "    "), apply collapseEmptyElements (plan 02) so each <Attribute> self-closes, and prepend the XML prolog (plan 02). Preserve input slice order — never range a Go map. In attributemap_test.go add TestRenderAttributeMap (golden byte-compare).
  </action>
  <verify>
    <automated>go test ./internal/render/... -run TestRenderAttributeMap</automated>
  </verify>
  <acceptance_criteria>
    - RenderAttributeMap output equals testdata/golden/attribute-map.xml byte-for-byte (TestRenderAttributeMap exits 0)
    - The Attributes root declares xmlns and xmlns:xsi as literal string attributes; children carry bare local-name tags
    - Each <Attribute> renders self-closing with name and id
    - `go test ./internal/render/... -run TestRenderAttributeMap` exits 0
  </acceptance_criteria>
  <done>attribute-map.xml renders byte-identical to the golden fixture with self-closing, auto-escaped attribute elements in input order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AppIntegration.attributes → attribute-map.xml | Tenant-supplied attribute name/id strings marshal into XML attribute values |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | XML injection via a hostile attribute name/id (e.g. embedded `<`, `&`, `"`) | high | mitigate | encoding/xml struct marshaling auto-escapes every attribute string routed through a normal struct-tag field (D-05/RENDER-10). Adversarial coverage across all string fields is exercised in plan 06 (TestInjectionSafety). |
| T-04-02 | Tampering | attribute-map.xml change not reflected in config hash (reloadChanges=false) | medium | mitigate | D-08: attribute-map.xml bytes are included in Hash's input set (plan 02) so an attribute-only change forces a pod roll. |
</threat_model>

<verification>
- `go test ./internal/render/... -run TestRenderAttributeMap` exits 0
- Golden byte-compare passes against testdata/golden/attribute-map.xml
</verification>

<success_criteria>
- RENDER-03: attribute-map.xml renders correctly from the attribute list (ROADMAP crit 5, attribute-map portion)
</success_criteria>

<output>
Create `.planning/phases/01-shared-render-aggregation-package/01-04-SUMMARY.md` when done
</output>
